{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nanotron.serviceAccountName" . }}
  labels: {{- include "nanotron.labels" . | nindent 4 }}
{{- end }}
